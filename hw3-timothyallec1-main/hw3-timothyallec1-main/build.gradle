/*
 * Copyright (c) 2021.
 * California Baptist University
 * Department of  Computer Science and Data Sciences
 * build.gradle
 *
 * Last Modified Date: 5/25/21, 3:27 PM
 * Last Modified User: chasecrossley
 */

/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn how to create Gradle builds at https://guides.gradle.org/creating-new-gradle-builds
 */


// Apply the java plugin to add support for Java
apply plugin: 'java'

// Apply the 'eclipse' plugin so we can generate Eclipse project files
apply plugin: 'eclipse'

// Apply the 'idea' plugin so we can generate IntelliJ project files
apply plugin: 'idea'

// This project uses a few 3rd party libraries. Rather then downloading
// and installing them manually, which can be highly error-prone, we
// have Gradle do it for us. In the "repositories" section, we specify
// where we want to search for these 3rd party packages.
repositories {
    // Maven central is a website containing a large collection of
    // 3rd party libraries. It was originally meant to be used with the
    // Maven build tool, but it's so comprehensive other build tools decided
    // to just use this one instead of building a similar website.
    //
    // mavenLocal() refers a folder in your computer that contains any
    // libraries you may have already pre-downloaded; mavenCentral refers to
    // the actual website.
    mavenLocal()
    mavenCentral()
}


// Here, we list all the different libraries we plan on using.
// Gradle will automatically download them from the repositories listed above.
dependencies {
    implementation 'junit:junit:4.13.1'
    implementation 'org.junit.jupiter:junit-jupiter:5.7.0'
}

// Here, we list some libraries that we use to augment this build script
// itself. Currently, we're augmenting gradle to make it easier for us
// to build and run GUI programs.
buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

sourceSets.main.java.srcDirs = ['src']
sourceSets.test.java.srcDirs = ['tst']

//testSet.main.java.tstDirs = ['tst']
// Log stdout and stderr in tests
test {
    testLogging {
        events = ["passed", "skipped", "failed"]
        exclude '*CreatePngTest*'
        showStandardStreams = true
        exceptionFormat = "full"
    }
    reports {
        html.enabled = false
        junitXml.enabled = true
    }

    doFirst {
        try {
            tasks.generateAbbieFNames.executeTests()
        }
        catch (ignored) {
        }

        try {
            tasks.generateDonMNames.executeTests()
        }
        catch (ignored) {
        }

        try {
            tasks.generateMichelleFNames.executeTests()
        }
        catch (ignored) {
        }

        try {
            tasks.generateStuartMNames.executeTests()
        }
        catch (ignored) {
        }

        try {
            tasks.generateEthelFNames.executeTests()
        }
        catch (ignored) {
        }
        try {
            tasks.generateAbbieFNames2.executeTests()
        }
        catch (ignored) {
        }

        try {
            tasks.generateDonMNames2.executeTests()
        }
        catch (ignored) {
        }

        try {
            tasks.generateMichelleFNames2.executeTests()
        }
        catch (ignored) {
        }

        try {
            tasks.generateStuartMNames2.executeTests()
        }
        catch (ignored) {
        }

        try {
            tasks.generateEthelFNames2.executeTests()
        }
        catch (ignored) {
        }
    }
}

task generateAbbieFNames(type: Test) {
    filter {
        includeTestsMatching 'CreatePngTest.generateAbbieFNames'
    }
    useJUnitPlatform()
}

task generateDonMNames(type: Test) {
    filter {
        includeTestsMatching 'CreatePngTest.generateDonMNames'
    }
    useJUnitPlatform()
}

task generateMichelleFNames(type: Test) {
    filter {
        includeTestsMatching 'CreatePngTest.generateMichelleFNames'
    }
    useJUnitPlatform()
}

task generateEthelFNames(type: Test) {
    filter {
        includeTestsMatching 'CreatePngTest.generateEthelFNames'
    }
    useJUnitPlatform()
}

task generateStuartMNames(type: Test) {
    filter {
        includeTestsMatching 'CreatePngTest.generateStuartMNames'
    }
    useJUnitPlatform()
}

task generatePictures(type: Test) {
    filter {
        includeTestsMatching 'CreatePngTest.*'
    }
    useJUnitPlatform()
}

//task testNames(type: Test) {
//    filter {
//        includeTestsMatching 'TestBabyNames.*Names'
//    }
//    testLogging {
//        events = ["passed", "skipped", "failed"]
//        exclude 'CreatePngTest*'
//        showStandardStreams = false
//        exceptionFormat = "full"
//    }
//
//    doFirst {
//        try {
//            tasks.generateAbbieFNames.executeTests()
//        }
//        catch (ignored) {
//        }
//
//        try {
//            tasks.generateDonMNames.executeTests()
//        }
//        catch (ignored) {
//        }
//
//        try {
//            tasks.generateMichelleFNames.executeTests()
//        }
//        catch (ignored) {
//        }
//
//        try {
//            tasks.generateStuartMNames.executeTests()
//        }
//        catch (ignored) {
//        }
//
//        try {
//            tasks.generateEthelFNames.executeTests()
//        }
//        catch (ignored) {
//        }
//    }
//
//    reports {
//        html.enabled = true
//        junitXml.enabled = true
//    }
//}

task generateAbbieFNames2(type: Test) {
    filter {
        includeTestsMatching 'CreatePngTest.generateAbbieFNames2'
    }
    useJUnitPlatform()
}

task generateDonMNames2(type: Test) {
    filter {
        includeTestsMatching 'CreatePngTest.generateDonMNames2'
    }
    useJUnitPlatform()
}

task generateEthelFNames2(type: Test) {
    filter {
        includeTestsMatching 'CreatePngTest.generateEthelFNames2'
    }
    useJUnitPlatform()
}

task generateMichelleFNames2(type: Test) {
    filter {
        includeTestsMatching 'CreatePngTest.generateMichelleFNames2'
    }
    useJUnitPlatform()
}

task generateStuartMNames2(type: Test) {
    filter {
        includeTestsMatching 'CreatePngTest.generateStuartMNames2'
    }
    useJUnitPlatform()
}

//task testNames2(type: Test) {
//    filter {
//        includeTestsMatching 'TestBabyNames.*Names2'
//    }
//    testLogging {
//        events = ["passed", "skipped", "failed"]
//        exclude 'CreatePngTest*'
//        showStandardStreams = true
//        exceptionFormat = "full"
//    }
//
//    doFirst {
//        try {
//            tasks.generateAbbieFNames2.executeTests()
//        }
//        catch (ignored) {
//        }
//
//        try {
//            tasks.generateDonMNames2.executeTests()
//        }
//        catch (ignored) {
//        }
//
//        try {
//            tasks.generateMichelleFNames2.executeTests()
//        }
//        catch (ignored) {
//        }
//
//        try {
//            tasks.generateStuartMNames2.executeTests()
//        }
//        catch (ignored) {
//        }
//
//        try {
//            tasks.generateEthelFNames2.executeTests()
//        }
//        catch (ignored) {
//        }
//    }
//    reports {
//        html.enabled = true
//        junitXml.enabled = false
//    }
//
//}

tasks.withType(Test) {
    logging.captureStandardOutput LogLevel.QUIET
    logging.captureStandardError LogLevel.QUIET
}
